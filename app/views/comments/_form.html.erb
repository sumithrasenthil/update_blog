<h3 class ="post-align">Post a Comment</h3>

<%= form_for [ @article, @comment ] do |f| %>
  
  <%= f.hidden_field :user_id, value: current_user.id %>

  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  
  
  
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>